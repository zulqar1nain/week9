#include <iostream>
using namespace std;
main()
{
    string name;
    getline(cin,name);
    int idx=0;
    string found;
    while(name[idx]!='\0')
    {
      found=name;
    idx++;
    }
    
   cout<<"Something"<<" "<<found;
}